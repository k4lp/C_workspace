#include<stdio.h>
#include<stdlib.h>

#define SIZE 100

typedef struct _node {
	int data[SIZE];
	int top;
} node;

node *pu = NULL;

void initialize() {
	pu = (node *)calloc(1,sizeof(node));
	pu->top = -1;
}

